# If the cpm output fails because the inventory.nix is incomplete, please:
change only the example inventory so the model stays strict and the example becomes explicit instead of synthesized.
# if the CPM output fails because the network-forwarding-model is incomplete, crash and state what should be in the nfm.
