# network-control-plane-model Regression Notes

This file records current policy exceptions only. Keep entries exact and
current; do not use it as a session log.

## Nix File LOC States

The file-size guard requires every tracked Nix file over the soft limit to have
a current state and reason. Files at or above the hard limit fail immediately
and must be split before tests can pass.

<!-- nix-file-loc:start -->
483 invariants/default.nix | state=watch | reason=invariant entrypoint still coordinates forwarding-model input checks and CPM output checks after shared assertion helpers were extracted
480 src/cpm/resolve-policy-endpoint-bindings.nix | state=watch | reason=extract relation endpoint parsing from runtime binding projection before adding new policy behavior
333 src/cpm/resolve-firewall-intent.nix | state=watch | reason=split per-family NAT target assembly from forwarding-entry wiring before adding more firewall behavior
414 src/cpm/build-site-data.nix | state=watch | reason=split route-augmentation orchestration, runtime-target construction, default-reachability assembly, and output projection into separate Site build modules before adding more site orchestration
430 src/cpm/validate-forwarding-model.nix | state=watch | reason=extract schema checks from semantic checks before adding new forwarding-model validation
311 flake.nix | state=watch | reason=flake app wiring remains below hard limit and owns CLI/test entrypoint assembly
255 src/cpm/Site/default-reachability/route-synthesis.nix | state=watch | reason=split overlay delegated egress synthesis from final default-route classification before adding more default reachability behavior
261 src/cpm/Site/default-reachability/source-selection.nix | state=watch | reason=split default-source detection from delegated runtime-prefix owner classification before adding more route-source classes
218 src/cpm/ControlModule/dns-policy/derived-services.nix | state=watch | reason=split DNS access/egress derivation helpers before adding more DNS service policy behavior
<!-- nix-file-loc:end -->

## Architecture Shape

- state=implemented-in-progress | target=Enterprise/Site/Unit/EquipmentModule/ControlModule layout | reason=CPM modules must follow the same responsibility-oriented shape as renderers so site assembly, unit advertisements, realization inventory, and control-plane projection do not collapse into large cross-layer files.
- state=hard-guard | target=`tests/test-structural-keyword-boundary.sh` | reason=Repeated role words (`access`, `policy`, `core`, `upstream-selector`, `downstream-selector`), selector/runtime model words, protocol abbreviations (`p2p`, `bgp`, `dns`, `wan`, `ipam`, `ra`, `pd`, `asn`, `rr`, `vlan`), tenant/service/lane names, generated-name fragments (`link::`, `adj::`, `--access-`, `--uplink-`), parser primitives (`builtins.match`, `builtins.split`, `hasInfix`, `suffixAfter`), and concrete lab identities such as `esp0xdeadbeef` in implementation logic mean files are parsing generated names and lab-specific strings instead of consuming one structured S88-style projection. This hard failure is required because scattered role/site/protocol parsing creates multiple local interpretations of the same CPM output and can miss route families, DNS lanes, BGP rows, or deny-policy rows when the compiled shape changes. Keywords are acceptable as include/import boundaries; other implementation occurrences must be replaced by segmented modules and linear structured data flow.
- state=guard | target=IPv6 parsing and range checks | reason=CPM must use the pinned `lib.network.ipv6` supplied by the `nixpkgs-network` flake override for IPv6 parsing/address math; do not add local IPv6 parsers or 128-bit integer arithmetic.
- state=guard | target=DNS forwarder runtime facts | reason=CPM must reject unresolved DNS placeholder strings before renderer output; runtime/SOPS inventory has to resolve forwarders before control-plane construction.
- state=verified-passing | target=`tests/test-network-labs-inventory-sweep.sh` | reason=The locked sweep compiles every network-labs example plus lab-sigma NixOS/CLAB inventories and checks DNS, BGP, policy, route, shape, and service-provider endpoint contracts across 46 outputs.
- state=fixed-locally | target=policy deny, BGP advertisement, router-self DNS, and DNS access route contracts | reason=Policy targets emit relation-derived deny rows, BGP access routers emit explicit `bgp.networks.ipv4`/`ipv6`, explicit access advertisements that publish router-self DNS become Unbound service contracts, and access DNS services emit route-contract plus exact P2P route rows for modeled forwarders so renderers do not infer policy, advertisements, or DNS paths.
- state=fixed-locally | target=DNS public resolver egress class derivation | reason=DNS services with intent-derived tenant/service DNS-to-external allowance now carry `explicit-egress-default`; the jq report also distinguishes explicit `false` from missing fallback policy instead of treating `false // true` as allowed fallback.
- state=fixed-locally | target=delegated IPv6 public-egress defaults | reason=Delegated IPv6 access lanes now strip non-overlay `::/0` defaults while preserving non-delegated WAN defaults and core underlay defaults.
- state=fixed-locally | target=site-c overlay-ingress delegated IPv6 public egress | reason=Runtime-routed IPv6 tenant prefixes now cause CPM to place a policy-only delegated-public-egress default on the upstream selector overlay-core ingress toward the owning tenant east-west policy lane, preventing no-route failures after Nebula decapsulation while keeping the policy lane from becoming a second default source.
- state=fixed-locally | target=delegated overlay public-egress source classification | reason=Delegated overlay public-egress synthesis now requires the site to have an explicit overlay default source. This preserves the hostile branch east-west public-exit lane while preventing site-C `c-router-nebula-core` from installing a public `::/0` back into Nebula when site-C public egress belongs on its WAN core. Covered by `tests/test-delegated-overlay-public-egress.sh`.
- state=fixed-locally | target=S88 policy-router firewall projection | reason=Policy firewall projection now maps modeled service endpoints onto same-access policy uplink lanes, so relation-derived service denies become explicit `forwardingIntent.rules` instead of disappearing before renderer tests.
- state=fixed-locally | target=overlay provider payload preservation | reason=CPM overlay provisioning now preserves provider-specific overlay payloads through a generic provider-name lookup instead of spelling provider names or interpreting provider concepts such as lighthouses in CPM source. Only provider-neutral `underlayEndpoints` and `underlayEndpointSourceFiles` are consumed for route/runtime endpoint hints. `tests/test-overlay-provisioning-provider-neutral.sh` guards the implementation boundary, while `tests/test-nebula-overlays-have-provider-contract.sh` verifies the existing provider payload still reaches renderer-facing CPM output.
- state=fixed-locally | target=transit-role default route classification | reason=CPM now marks synthesized default routes on policy/upstream-selector/downstream-selector roles as `policyOnly = true`, so lane defaults are rendered through policy routing instead of relying on renderer-side main-table defaults. `tests/test-transit-default-routes-are-classified.sh` now requires this for the focused prod-risk examples.
- state=still-broken | target=core DNS service/listener contract | reason=2026-05-14 live probes showed access routers receive explicit Kea/radvd/Unbound services and advertise router-self DNS to attached clients, while sampled core routers in `s-router-test` and Hetzner have no Unbound or modeled DNS listener. Cores do not inherently need Kea/radvd; CPM needs an explicit contract for any core or service node that policy may select as a DNS target, so renderers can materialize Unbound/listeners/routes without role-name inference.
- state=fixed-locally | target=DNS service outgoing source/lane contract | reason=2026-05-14 Hetzner live probe showed access-router Unbound listeners were active but DNS queries timed out because outbound forwarding selected transit/p2p source addresses. CPM now preserves explicit `services.dns.outgoingInterfaces` and derives tenant listener addresses as DNS egress sources for forwarding DNS services. Focused regression `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-policy-routes.sh` requires site-C DMZ and site-B hostile DNS services to emit their tenant IPv4/IPv6 source addresses.
- state=fixed-locally | target=access DNS forwarder route contracts | evidence=2026-05-14 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-network-labs-inventory-sweep.sh` passed for 44 locked network-labs outputs after focused DNS regressions passed | reason=CPM now materializes explicit `/32` and `/128` DNS forwarder host routes on access p2p interfaces from modeled DNS forwarders/route contracts without falling back to invented p2p peer gateways.
- state=fixed-locally | target=runtime tenant interface identity | evidence=2026-05-14 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-network-labs-inventory-sweep.sh` passed for 44 locked network-labs outputs | reason=Pipeline contract audit found renderers could fall back to runtime node-name token parsing when tenant interfaces lacked an explicit tenant field. CPM now preserves tenant identity on realized tenant interfaces so renderers can consume structured CPM output.
- state=fixed-locally | target=DNS route augmentation target classification | evidence=2026-05-14 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-policy-routes.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-derived-dns-upstreams.sh`, and `bash tests/test-no-dns-route-peer-fallback.sh` passed | reason=Pipeline contract audit found DNS route augmentation detected upstream selectors by rendered interface-name prefixes such as `core-` and `policy-`. CPM now uses the explicit runtime target role instead of interface-name inference.
- state=fixed-locally | target=hostile delegated IPv6 public egress and return routing | evidence=2026-05-14 live full-loop failure was narrowed with temporary route replacements on `b-router-upstream-selector`, `b-router-core-nebula`, Hetzner `c-router-upstream-selector`, `c-router-core`, and `c-router-nebula-core`; after adding the branch core-nebula return route, Hetzner core return routes, a site-c source-prefix-scoped core-nebula -> core allowance, and a `c-router-nebula-core` overlay return route for the branch delegated prefix, `hostile-node01` pinged `2606:4700:4700::1111` successfully. Focused tests now passing: `bash tests/test-runtime-routed-prefixes-no-validation-shortcut.sh`, `bash tests/test-delegated-overlay-public-egress.sh`, and `bash tests/test-upstream-selector-no-core-crossconnect.sh`. | reason=CPM must carry inventory-derived runtime routed prefixes across sites, synthesize the needed return routes, and emit a sourceFile-scoped public-egress firewall contract instead of sending overlay ingress to a tenant policy lane, returning remote delegated prefixes back to the upstream selector from a Nebula core, or opening a broad core cross-connect.
- state=fixed-locally | target=site-c upstream-selector remote delegated-prefix return lane | evidence=2026-05-14 full-loop hostile endpoint probe failed until live hotpatch replaced `c-router-upstream-selector` return route for `2a01:4f9:c01f:4186::/64` from `pol-client-ew` to `core-nebula`; `hostile-node01` then pinged `2606:4700:4700::1111`. Focused regression `bash tests/test-delegated-overlay-public-egress.sh` now requires site-C upstream selector to return the branch hostile delegated prefix toward `p2p-c-router-nebula-core-c-router-upstream-selector` and not toward the local client policy lane. | reason=Remote runtime-routed prefixes on an upstream selector must be returned to the overlay uplink lane, not to any local access-uplink lane that happens to use the overlay.
- state=fixed-locally | target=per-prefix IPv6 NAT intent | evidence=2026-05-14 full-loop `tests-from-s-router-test-clients` failed on `branch-node01` IPv6 public egress while IPv4 worked; live hotpatching `ip6 nat postrouting oifname eth0 masquerade` on `b-router-core-simulated-isp` immediately made branch IPv6 ping/curl to Cloudflare succeed. CPM had disabled NAT66 for the whole site because hostile has a routed public IPv6 prefix, even though branch is ULA-only. `tests/test-hostile-dns-east-west.sh` now requires `b-router-core-simulated-isp.natIntent.families.ipv6 = true` and `masqueradeInterfaces6 = [ "wan" ]`. | reason=NAT66 eligibility is per private IPv6 source class, not a site-wide off switch whenever another tenant has a routed public prefix.

## Live Lab Failures

- state=still-broken | target=branch-to-site-A DNS is not a p2p contract failure | evidence=2026-05-14 late live debugging walked branch DNS to site-A mgmt DNS `10.20.10.1` hop by hop. `ip route get ... iif <p2p>` selected the intended branch access -> downstream selector -> policy -> upstream selector -> branch Nebula core -> site-A Nebula core -> upstream selector -> policy -> downstream selector -> access-mgmt path in both directions. The intermittent DNS loss happened after the correct route decision, with replies visible on `s-router-core-nebula` but not consistently on `b-router-core-nebula` over Nebula. | reason=CPM should keep the relation-derived branch-to-mgmt DNS route and firewall contract explicit, but this symptom should not be fixed by inventing new CPM p2p routes. The owning durable work is provider/runtime and renderer materialization after CPM has emitted the policy lane.
- state=still-broken | target=hostile egress proof requires validator presence and tenant-origin route contract | evidence=2026-05-14 `hostile-node01` endpoint had expected tenant IPv4 and delegated IPv6 addresses, but IPv4 public ping/DNS and IPv6 public route-get failed; the Hetzner validator was later confirmed absent and `s-router-test` became unreachable. | reason=CPM tests should continue to require explicit hostile public-exit and return-path contracts for tenant-origin prefixes, but live validation must not reinterpret host-origin or router-origin traffic as CPM success. Absence of the external validator makes public-exit proof unavailable, not green.
- state=still-broken | target=core-to-core route isolation symptom | evidence=2026-05-14 live Hetzner route checks showed `c-router-core` selects routes to `c-router-nebula-core` loopbacks via `c-router-upstream-selector`, and `c-router-nebula-core` selects routes back to `c-router-core` loopbacks via the same selector. ICMP was blocked, but route selection still exposes direct core-to-core reachability intent outside policy routers. | reason=This is recorded here as downstream evidence only; the owning invariant is now documented in `network-compiler/regression.md` because core-to-core p2p/reachability must be prevented at the structural contract boundary.
- state=still-broken | target=2026-05-13 fast visible-scope refresh | evidence=Fast live matrix `/tmp/s-router-fast-enum-20260513T212251Z/summary/fast.tsv` captured `s-router-test`, `s-router-test-clients`, and `s-router-clab` while Hetzner was down/not visible. It confirms branch/hostile DNS and public egress are still broken, regular client IPv4/IPv6 egress is mixed by lane, selector/policy routers have no direct host-origin public route, and CLAB is currently stale/unusable. | reason=The visible scope is enough to keep route/default classification, lane preservation, and DNS/firewall contracts as production blockers; Hetzner-specific validation must be rerun through the full loop when a validator exists.
- state=still-broken | target=2026-05-13 live enumeration gate | evidence=Compact live matrices were captured for 19 `s-router-test` router containers, 9 `s-router-test-clients` endpoint containers, and 8 Hetzner validator containers. Address, route-get, DNS, public IPv4 leak classification, public IPv6 curl, and nft hook summaries are in `/tmp/s-router-test-compact.tsv`, `/tmp/s-router-test-clients-compact.tsv`, and `/tmp/s-router-hetzner-compact.tsv` on the operator box. | reason=Enumeration is now broad enough to show this is not a single DNS bug; public egress, route selection, DNS reachability, and policy table behavior differ per lane/site and must be fixed through CPM/renderer contracts before prod-ready validation.
- state=still-broken | target=hostile delegated public egress lane | evidence=2026-05-13 hostile-node01 traceroute to 2606:4700:4700::1111 looped between fd42:dead:beef:ee::3 and fd42:dead:beef:ee::2 after access -> policy -> upstream-selector -> core-nebula; traceroute to 1.1.1.1 reached only 10.70.10.1 and then timed out | reason=CPM/provider route ownership must prove delegated hostile defaults only enter the overlay on the source side and that hostile IPv4 has a modeled public-exit lane.
- state=still-broken | target=hostile delegated public egress current compact probe | evidence=2026-05-13 hostile-node01 has delegated GUA `2a01:4f9:c01f:4186::/64`, `ip -6 route get 2606:4700:4700::1111` selects the access-router RA default and the delegated GUA source, but DNS query timed out and IPv4/IPv6 public curl both failed; b-router-core-nebula selects `nebula1` for public IPv6 and c-router-nebula-core also selects `nebula1` for public IPv6. | reason=The live route shape still supports the observed overlay loop and failed egress/return path.
- state=still-broken | target=client2 IPv4 public egress | evidence=2026-05-13 client2-test resolved example.com and reached 2606:4700:4700::1111 over IPv6, but ping to 1.1.1.1 had 100% packet loss | reason=IPv4 and IPv6 public egress are not consistently modeled across regular client lanes.
- state=still-broken | target=client and management public-egress consistency | evidence=2026-05-13 compact client matrix: client-test had IPv4 public egress classified as OTHER and IPv6 public curl passed; client2-test, admin-test, and mgmt-test had IPv6 public curl pass but IPv4 public curl fail; dmzweb01, nebula01, wireguard01, branch-node01, and hostile-node01 had IPv4 and IPv6 public curl fail. | reason=The model currently produces mixed egress behavior across lanes; each lane needs an explicit intended public-egress contract before prod validation can classify pass/fail.
- state=still-broken | target=branch public egress | evidence=2026-05-13 branch-node01 failed ping to 1.1.1.1 and failed ping to 2606:4700:4700::1111; route lookup selected the RA default on eth0 for IPv6 | reason=branch access public egress still needs owning-layer route/policy classification before production-ready validation.
- state=still-broken | target=home-public-ip leak prevention | evidence=2026-05-13 redacted live probe used the current caller public IPv4 without printing it; client-test, b-router-core-simulated-isp, Hetzner c-router-core, and Hetzner c-router-lighthouse could ping that address, while hostile-node01, client2-test, branch-node01, admin-test, mgmt-test, dmzweb01, nebula01, wireguard01, b-router-access-hostile, b-router-core-nebula, s-router-access-client, s-router-access-client2, s-router-core-nebula, Hetzner c-router-nebula-core, c-router-policy, c-router-upstream-selector, c-router-access-client, and c-router-access-dmz could not | reason=production-safe validation requires explicit leak-prevention policy for the caller/home public address class without committing the concrete address.
- state=still-broken | target=site-c public egress | evidence=2026-05-13 Hetzner c-router-nebula-core, c-router-policy, c-router-upstream-selector, c-router-access-client, and c-router-access-dmz all failed public IPv4 and IPv6 probes; c-router-core and c-router-lighthouse had IPv4 public reachability but failed IPv6 public ping | reason=site-C route/policy/default-reachability contracts are not consistently selecting usable public egress.
- state=still-broken | target=site-c DNS and direct public DNS posture | evidence=2026-05-13 Hetzner compact probe: c-router-access-client DNS timed out, c-router-access-dmz DNS failed, c-router-lighthouse DNS timed out, and direct `dig @1.1.1.1` was blocked from c-router-access-client/c-router-access-dmz but succeeded from c-router-lighthouse. | reason=Endpoint/service DNS policy is inconsistent on the Hetzner side and needs a modeled decision about whether service endpoints may bypass access DNS.
- state=still-broken | target=Nebula overlay vs public route classification | evidence=2026-05-13 `s-router-core-nebula`, `b-router-core-nebula`, and `c-router-nebula-core` all had active `nebula-runtime.service` and successful IPv4/IPv6 pings across `nebula1`; `c-router-lighthouse` had active service but no `nebula1` address and overlay pings to site-A timed out through its tenant/default route. | reason=The overlay daemon itself is reachable on core nodes, so the hostile/site-C failure is route/policy classification around public/default traffic and endpoint placement, not a blanket Nebula process outage.
- state=fixed-locally | target=default-reachability generated-name inference | evidence=2026-05-14 source audit found `src/cpm/Site/default-reachability/route-helpers.nix` parsing generated interface names with `interfaceNameTargetsDestination` and `interfaceNameHasUplinkWanPreference`, `src/cpm/Site/default-reachability/context.nix` reconstructing overlay names from interface names, and default route synthesis falling back to `--access-`/`--uplink-` adjacency parsing. Those fallbacks were removed; default reachability now uses structured `laneMeta` and realized `backingRef.lane`/`backingRef.name` data, and `tests/test-no-default-reachability-name-inference.sh` guards the old helper/pattern names. | reason=CPM must join explicit NFM output with inventory, not become a second forwarding solver.
- state=fixed-locally | target=CPM DNS route fallback audit | evidence=2026-05-14 source audit found DNS route augmentation fabricating `routeViaPeer` fallbacks when exact route coverage was absent, and runtime DNS contracts also installed forwarder routes via p2p peers. `src/cpm/ControlModule/route-augmentation/dns/source-routes.nix` now reuses only explicit exact, covering, or default routes already present on realized interfaces, `src/cpm/ControlModule/runtime-targets/dns-contracts.nix` keeps DNS route contracts without mutating interface routes, and `tests/test-no-dns-route-peer-fallback.sh` blocks p2p peer-gateway synthesis from returning. | reason=CPM must not invent DNS reachability; missing route coverage belongs in NFM/model output or inventory realization, not local route augmentation.
