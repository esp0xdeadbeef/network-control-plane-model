- state=fixed-locally | target=overlay-underlay-access-dynamic-client-addressing | evidence=2026-05-24 live `nixos-router-core-nebula` after the p2p removal had only `client@if58`; no stale `access-client` or `upstream` p2p interfaces leaked into the underlay. The rendered `/etc/systemd/network/10-client.network` had `IPv6AcceptRA=false`, `LinkLocalAddressing=no`, and no DHCP setting, so `client` had no IPv4/IPv6 address, `ip route get 1.1.1.1` returned `Network is unreachable`, and the overlay service was not started. Replacing that one networkd unit with DHCPv4 plus IPv6 RA made the node acquire `10.20.20.102/24` and SLAAC addresses from the client tenant LAN. CPM now emits explicit `dynamicAddressing.ipv4.method = "dhcp"` and `dynamicAddressing.ipv6.method = "slaac"` on host-like logical tenant attachments such as `esp-nixos-router-core-nebula.tenant-client` and `esp-clab-router-core-nebula.tenant-client`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-dynamic-client-addressing.sh` proves the lab-sigma resolved NixOS and CLAB inventories carry that contract. | reason=The overlay underlay core is now modeled as a tenant client with dynamic addressing, not as a static p2p link. CPM owns the renderer-independent runtime interface contract, including whether a logical tenant attachment should use DHCP/SLAAC when no static addresses are present. Renderers must consume this explicit field instead of inferring dynamic behavior from missing `addr4`/`addr6`, runtime names, or local lab topology.

- state=fixed-locally | target=runtime-origin-dns-deny-precedence-and-nat66-source-scope | evidence=2026-05-24 live `nixos-router-core-nebula` could ping IPv4 from source `10.19.0.8`, but `dig google.com` through local Unbound timed out/SERVFAIL. `tcpdump` showed recursive DNS packets sourced from `10.19.0.8` entered `nixos-router-policy` on `downstr-client`; `nft list ruleset` showed `deny-tenant-dns-to-uplinks` UDP/TCP 53 drops on `downstr-client -> up-client-{a,b}` before later source-scoped `runtime-origin-egress` accepts for `10.19.0.8/32` and `fd42:dead:beef:1900::8/128`. The same live rebuild showed core-nebula IPv6 underlay route selected `fd42:dead:beef:1900::8`, while core NAT66 source sets included only tenant ULA /64s and not the runtime-origin ULA /128. `tests/test-runtime-origin-loopback-egress.sh` now fails if runtime-origin source accepts do not sort before same-lane DNS denies or if explicit NAT66 exits omit runtime-origin ULA /128 source prefixes. | reason=CPM owns renderer-independent policy precedence and NAT source scope once it emits runtime-origin egress. Tenant direct-DNS leak denies must stay intact, but they must not shadow router runtime-origin recursion/bootstrap traffic. Explicit NAT66 exits may translate ULA runtime-origin sources in simulated/non-routed IPv6 uplinks, while routed GUA prefixes must remain routed and must not be folded into NAT66.

- state=fixed-locally | target=runtime-origin-policy-uplink-crossconnect | evidence=2026-05-24 locked full-loop with CPM `7af82ba` rebuilt successfully but live `nixos-router-core-nebula ping -4 -c2 1.1.1.1` still failed. `gron /etc/network-artifacts/control-plane.json | rg 'runtime-origin-egress|up-client-a|up-client-b|10\\.19\\.0\\.8'` on `nixos-router-policy` showed CPM emitted `runtime-origin-egress` accepts from `up-client-a -> up-client-b` and `up-client-b -> up-client-a` for the core-nebula loopback source. The NixOS renderer then materialized those explicit forwarding rules into `/etc/systemd/network/10-up-client-{a,b}.network` source-reachability routes to `10.19.0.8/32` and `fd42:dead:beef:1900::8/128`, so WAN replies selected policy access-uplink main routes instead of the modeled downstream-client return route. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress.sh`, `tests/test-upstream-selector-nebula-underlay-core-transit.sh`, `tests/test-policy-source-scope-preserved.sh`, `tests/test-example-overlay-upstream-policy-defaults-preserved.sh`, and `tests/test-selector-forwarding-opaque-runtime-names.sh` pass after CPM restricts policy runtime-origin default forwarding to access/downstream ingress lanes and access-uplink egress lanes. | reason=CPM must not model runtime-origin forwarding between sibling policy access-uplink lanes. The only valid policy-node runtime-origin egress leg is from the origin access/downstream lane toward eligible access-uplink lanes; continuation toward cores is owned by the upstream-selector policy-to-core contract. Renderers must not delete explicit CPM forwarding rules to compensate.

- state=fixed-locally | target=downstream-selector-access-policy-tenant-egress | evidence=2026-05-24 live `nixos-client01 traceroute 1.1.1.1` reached `_gateway (10.20.20.1)` and downstream `10.10.0.5`, then only `* * *`. Live route selection on `nixos-router-downstream` selected `via 10.10.0.23 dev policy-client table 2001` for `from 10.20.20.100 iif access-client`, but live nft only had `access-client -> policy-client` accepts scoped to runtime-origin loopback prefixes such as `10.19.0.8/32`, not ordinary tenant prefixes. `gron /tmp/s-router-live-artifacts-now/control-plane.json | rg 'esp-nixos-router-downstream|access-client|policy-client|sourcePrefixes'` confirmed CPM emitted source-scoped runtime-origin downstream selector rules but no unscoped `access-client -> policy-client` selector pair for normal tenant egress. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-selector-forwarding-opaque-runtime-names.sh` now covers both downstream access-policy and upstream policy-core selector pairs with opaque runtime interface names. | reason=CPM owns renderer-independent selector forwarding intent. NFM already emitted the route; renderers must receive explicit unscoped downstream access-lane to policy-lane firewall intent for normal tenant traffic and must not infer it from routes, runtime interface names, or lab topology. Runtime-origin loopback source rules are an additional narrow bootstrap contract, not a replacement for normal tenant egress.

- state=fixed-locally | target=upstream-selector-policy-core-tenant-egress | evidence=2026-05-24 full-loop live validation failed normal client public egress while streaming egress worked. Live route probes showed `clab-router-policy ip route get 1.1.1.1 from 10.50.20.10 iif eth2` selected the client policy uplink and `clab-router-upstream ip route get 1.1.1.1 from 10.50.20.10 iif eth4` selected `core-simulated-isp`; NixOS similarly selected `nixos-router-upstream` `pol-client-b -> core-b`. Live nft on `nixos-router-upstream` had `policy drop` and only `runtime-origin-egress` source-scoped `pol-client-* -> core-*` accepts for `10.19.0.8/32`, so normal tenant packets were dropped after the correct route selection. `gron /tmp/s-router-artifacts-current/nixos-control-plane.json | rg 'nixos-router-upstream|pol-client|core-b|sourcePrefixes'` confirmed CPM emitted the runtime-origin source-scoped pair but no unscoped policy-lane/core-lane pair for ordinary tenant egress. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-selector-forwarding-opaque-runtime-names.sh`, `tests/test-upstream-selector-nebula-underlay-core-transit.sh`, `tests/test-upstream-selector-no-core-crossconnect.sh`, and full `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=$(nproc) bash test-control-plane-model.sh` pass after CPM emits explicit unscoped policy-side access/uplink lane to matching core uplink lane forwarding pairs while retaining the narrow runtime-origin source-scoped rules. | reason=CPM owns renderer-independent upstream-selector forwarding intent. Routes alone are not enough; renderers must receive explicit firewall intent for policy-lane to core-lane tenant egress and must not infer it from interface names, route tables, or local lab topology.

- state=fixed-locally | target=runtime-origin-origin-lane-scope | evidence=2026-05-23 full-loop live tcpdump showed `nixos-router-core-nebula` requests reached WAN through the client underlay, replies returned to `nixos-router-policy`, then policy forwarded replies out unrelated lanes such as `up-mgmt-b` instead of `downstr-client`. A later locked rebuild still failed: `nixos-router-core-nebula ping -4 -c1 1.1.1.1` lost replies, `nixos-router-policy ip route show table main` carried `10.19.0.8/32` via mgmt/admin/streaming/client/hostile lanes, and `gron /etc/network-artifacts/control-plane.json | rg '10\\.19\\.0\\.8|runtime-origin-egress|origin'` showed CPM `sourcePrefixes` still lacked `origin.accesses` when the default-bearing underlay was a direct p2p link with `lane = "default"`. CPM now resolves that origin by joining the default-bearing realized link to its explicit access-gateway peer target and emits the peer access in `sourcePrefixes[].origin.accesses`. Follow-up `gron` over `examples/s-router-overlay-dns-lane-policy` exposed a second unsafe fallback: CPM treated empty `origin.accesses = []` as a wildcard and sprayed runtime-origin source prefixes across every access lane. CPM now treats empty runtime-origin access origin as non-matching for access-scoped lane selection, so missing origin no longer becomes broad policy/nft authorization. A current locked full-loop attempt with CPM `594aa7c` proved `nixos-router-policy ip route get 1.1.1.1 from 10.19.0.8 iif downstr-client` was still unreachable because the overlay daemon source prefix carried no access origin at all. CPM now also derives the runtime-origin source access from explicit `transport.overlays[].underlayAccess` for the overlay uplinks listed in `runtimeOriginEgress.uplinks`, so `east-west` runtime-origin source scope maps to the modeled `client` underlay access instead of names or renderer heuristics. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress.sh`, `tests/test-upstream-selector-nebula-underlay-core-transit.sh`, and `tests/test-policy-source-scope-preserved.sh` pass. | reason=CPM owns the explicit renderer-independent source scope. The renderer must not decide that `downstream-client` is the return lane from names, but CPM must not hand renderers a source prefix scoped to every unrelated access lane when the modeled origin access can be resolved from explicit target functions, realized peer links, or the overlay underlay-access contract. If CPM cannot resolve origin access, the safe behavior is no access-lane match, not a wildcard.

- state=fixed-locally | target=runtime-origin-policy-core-continuation | evidence=2026-05-23 full-loop live validation left `nixos-router-core-nebula ping 1.1.1.1` dependent on temporary route/rule patches: `nixos-router-policy ip route get 1.1.1.1 from 10.19.0.8 iif downstr-client` failed until the modeled source-return route was present in main, and `nixos-router-upstream ip route get 1.1.1.1 from 10.19.0.8 iif pol-client-a` was unreachable until a source-scoped rule selected the existing client uplink table. `gron /etc/network-artifacts/control-plane.json | rg '10\\.19\\.0\\.8|runtime-origin-egress|pol-client-a'` showed CPM had runtime-origin facts on the origin/core-nebula lane but not on every policy-to-core continuation rule. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress.sh`, `tests/test-example-overlay-upstream-policy-defaults-preserved.sh`, `tests/test-upstream-selector-nebula-underlay-core-transit.sh`, `tests/test-policy-source-scope-preserved.sh`, and `tests/test-delegated-overlay-public-egress.sh` pass after CPM keeps runtime-origin loopback source scope on default-bearing policy lanes and upstream-selector policy-to-core lanes. | reason=CPM owns the explicit renderer-independent forwarding contract for loopback/runtime-origin bootstrap traffic. Renderers must receive source-scoped continuation rules and source-return routes from CPM; they must not infer them from `core-nebula`, `policy`, `upstream`, interface names, or local lab topology.

- state=fixed-locally | target=runtime-origin-policy-return-route | evidence=2026-05-24 locked full-loop live packet trace after CPM `9dcd5fb` showed `nixos-router-core-nebula` ICMP requests reached WAN and replies returned to `nixos-router-policy` on `up-client-a`, but `nixos-router-policy ip -4 route get 10.19.0.8 from 1.1.1.1 iif up-client-a` selected `via 10.10.0.37 dev up-client-a` instead of the downstream client lane. `gron /tmp/s-router-test-control-plane-9d-live.json | rg 'esp-nixos-router-policy|10\\.19\\.0\\.8|downstream-client|up-client'` and TSV extraction confirmed CPM emitted runtime-origin egress rules on `downstream-client -> up-client-*` but no non-policy route to `10.19.0.8/32` or `fd42:dead:beef:1900::8/128` on `downstream-client`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress.sh` now fails on that missing return route before the fix and passes after CPM adds non-policy runtime-origin return routes to policy access ingress lanes using the already explicit same-lane next hop. Related focused tests `tests/test-upstream-selector-nebula-underlay-core-transit.sh`, `tests/test-policy-source-scope-preserved.sh`, and `tests/test-example-overlay-upstream-policy-defaults-preserved.sh` pass. | reason=CPM owns the renderer contract for return reachability once it emits a source-scoped runtime-origin forwarding rule. A renderer must not invent the return route from interface names or install a broad main-table default; CPM must carry the specific runtime-origin source prefix back toward the modeled origin access lane.

- state=fixed-locally | target=runtime-origin-policy-uplink-return-route-suppression | evidence=2026-05-24 locked full-loop live validation after CPM `0f94dbb` proved the missing downstream return route was not enough: `nixos-router-core-nebula ip -4 route get 1.1.1.1` selected the modeled access-client underlay, `nixos-router-access-client ip route get 1.1.1.1 from 10.19.0.8 iif core-nebula` selected `via 10.10.0.5 dev transit table 2000`, and `nixos-router-policy ip -4 route get 1.1.1.1 from 10.19.0.8 iif downstr-client` selected `via 10.10.0.39 dev up-client-b table 2001`, but return lookup still failed because `nixos-router-policy ip -4 route get 10.19.0.8 from 1.1.1.1 iif up-client-a` and `iif up-client-b` selected access-uplink main routes instead of `downstr-client`. `gron /etc/network-artifacts/control-plane.json | rg 'esp-nixos-router-policy|10\\.19\\.0\\.8|runtime-origin-egress|up-client|downstream-client'` showed CPM emitted the runtime-origin source prefix both on the intended downstream-client return lane and as non-policy main routes on policy access-uplink lanes; a follow-up locked run with CPM `1c489cd` showed the original route filter was still incomplete because `policyTableComplements` reintroduced runtime-origin return routes such as `10.19.0.8/32` and `fd42:dead:beef:1900::8/128` on access-uplink attach targets with `reason = "policy-table-internal-reachability"`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress.sh` now fails if any policy access-uplink interface carries a route whose destination is a runtime-origin source prefix, and the focused route/selector tests pass after CPM suppresses original unsafe routes and prevents runtime-origin return routes from being copied into generated policy-table complements before renderer materialization. | reason=CPM owns normalized route semantics. A policy access-uplink lane may carry source-scoped runtime-origin forwarding/default rules, but it must not also carry a main-table return route to the runtime-origin source prefix because that creates an upstream self-return candidate and breaks loopback-sourced underlay egress. Renderers must not guess which duplicated main route to delete.

- state=fixed-locally | target=runtime-origin-loopback-egress-contract | evidence=2026-05-24 locked full-loop with renderer `0852dd4` still showed `nixos-router-core-nebula ip route get 1.1.1.1` selecting `via 10.10.0.2 dev access-client src 10.19.0.8`, while live `nixos-router-access-client` had a route for `10.19.0.8/32` on `core-nebula` and a default on `transit` but no nft accept for `core-nebula -> transit`. A later rebuild with CPM `d528922` still missed the rule because the live `core-nebula` p2p carried `backingRef.lane = "default"` rather than an access-lane object, so the route-based source scope existed but was excluded before rule construction. `gron /tmp/s-router-test-control-plane-after-build.json | rg 'esp-nixos-router-access-client|runtime-origin|core-nebula|transit|10\\.19\\.0\\.8'` confirmed CPM emitted `core-nebula -> tenant-client`, `tenant-client -> core-nebula`, and `tenant-client -> transit`, but no source-scoped `runtime-origin-egress` access-ingress rule from the runtime-origin p2p to the default-bearing transit p2p. `tests/test-runtime-origin-loopback-egress.sh` now includes a synthetic access-ingress shape with `backingRef.lane = "default"` that fails unless an interface with an explicit route to a runtime-origin source can forward that source to the default-bearing transit p2p. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-origin-loopback-egress.sh`, `tests/test-policy-source-scope-preserved.sh`, `tests/test-upstream-selector-nebula-underlay-core-transit.sh`, and `tests/test-example-overlay-upstream-policy-defaults-preserved.sh` pass. | reason=The model owns whether a router's local runtime identity may egress for provider/bootstrap work, and CPM owns the renderer-independent source-scoped forwarding contract for every hop that must carry that loopback source. Access ingress p2ps that route the runtime-origin loopback are valid ingress candidates even when they are not default-bearing interfaces and even when their lane is an unstructured `default` marker. Renderers should materialize explicit loopback source prefixes and preferred sources, not infer them from node names, DNS recursion settings, or the presence of an overlay provider.

- state=fixed-locally | target=core-dns-runtime-recursion-source-role | evidence=2026-05-22 live `s-router-test` proved the core overlay node can safely reach WAN only when traffic is sourced from its loopback/runtime identity (`10.19.0.8`) and enters `nixos-router-upstream` on `core-nebula`; the upstream interface source (`10.10.0.16`) correctly remains `Network is unreachable`. Local Unbound on `nixos-router-core-nebula` resolved through `127.0.0.1` after the route/preferred-source hotpatch, while direct `@1.1.1.1`/`@8.8.8.8` DNS stayed blocked by leak-prevention output rules. Upstream Unbound docs separate `interface`/`access-control` from `outgoing-interface`, so CPM now emits explicit `services.dns.roles.local` and `services.dns.roles.recursion` contracts. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-semantic-core-carry-through.sh`, `tests/test-access-dns-router-self-listeners.sh`, and `tests/test-dns-service-policy-routes.sh` pass, and `gron` over the current `s-router-overlay-dns-lane-policy` CPM output shows `s-router-core-nebula.services.dns.roles.recursion.outgoingInterfaces = [ "10.19.0.8", "fd42:dead:beef:1900:0:0:0:8" ]`. | reason=DNS policy is intent and CPM owns the renderer-independent service contract. Core/access DNS services need explicit roles such as listener scope, recursion source addresses, and allowed upstream classes so renderers can materialize Unbound/BIND/etc safely. This must not become a renderer name heuristic, a broad upstream TCP/UDP 53 allow, or a generic WAN default from the upstream-selector port. Full-loop live validation is still pending.

- state=fixed-locally | target=overlay-uplink-no-synthetic-host-port | evidence=2026-05-21 live `s-router-clab` showed `clab-router-core-nebula` had an unintended `eth2` on host uplink/WAN bridge `br-uplink1` with `10.13.0.127/24`; the lab inventory had added that direct port only because CPM rejected realized core-nebula nodes whose semantic `uplinks.east-west` lacked an explicit uplink port. CPM now skips synthetic uplink interface generation for overlay names, preserving the semantic east-west uplink on `overlay-east-west` and modeled p2p lanes instead of requiring a physical host-uplink port. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-delegated-overlay-public-egress.sh` passes with the forbidden inventory ports removed. | reason=CPM must not force overlay/east-west semantic uplinks into physical host uplink realization. The network model owns the staged fabric path; renderers should consume explicit overlay and p2p contracts, not receive a fake WAN-like port that can attach core-nebula directly to PPPoE/DHCP ISP/host bridges.

- state=fixed-locally | target=runtime-routed-prefix-public-egress-policy-traversal | evidence=2026-05-22 full-loop hostile public IPv6 egress failed after the renderer preserved forwarding-rule family scope: `nixos-hostile-node01 traceroute -6 2606:4700:4700::1111` reached the Hetz overlay, and live `hetz-router-upstream ip -6 route get ... iif core-nebula from 2a01:4f9:c01f:2::100` selected `via fd42:dead:cafe:1000::4 dev core table 2001`. A NixOS hotpatch replacing the hostile upstream policy-lane default with `via fd42:dead:beef:1000::10 dev core-nebula` moved traceroute through the overlay to Hetz, then Hetz failed because CPM still emitted an unscoped default from overlay ingress to core alongside the intended policy lane. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` now asserts the policy-lane default points toward the selected overlay/core peer and forbids the self-looping policy-peer default; it also asserts Hetz overlay ingress uses the east-west policy lane and does not retain the unscoped `core-nebula -> core` IPv6 default. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-delegated-overlay-public-egress.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-sitec-overlay-service-ingress.sh` pass after CPM sends runtime routed-prefix public egress from overlay-core ingress to the matching policy uplink lane and suppresses competing unscoped defaults. | reason=CPM owns explicit forwarding and route-table semantics. A delegated/routed public IPv6 source must still traverse the modeled policy path before WAN egress, and an upstream selector policy lane must default toward its selected uplink/core rather than back to the policy peer. Renderers must not receive a route set that contains both the intended policy lane and a bypass/self-loop candidate.
- state=fixed-locally | target=runtime-routed-prefix-policy-to-wan-sourcefiles | evidence=2026-05-22 full-loop rerun after the overlay default fix moved hostile IPv6 further, but `nixos-hostile-node01 traceroute -6 2606:4700:4700::1111` then died at Hetz `fd42:dead:cafe:1000::11 !N`. Live `hetz-router-upstream ip -6 route get ... iif policy-dmz-wan` returned `Network is unreachable`; adding only `ip -6 rule add from 2a01:4f9:c01f:2::/64 iif policy-dmz-wan lookup 2004 priority 2004` made hostile IPv6 ping Cloudflare pass. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` now requires CPM to carry routed-prefix `sourceFiles` onto the Hetz `policy-dmz-wan -> core` public-egress forwarding leg, not only the `core-nebula -> pol-dmz-ew` ingress leg. | reason=CPM owns the explicit multi-hop forwarding contract. If a routed public prefix enters over overlay, crosses policy, and exits WAN, every selector ingress leg that needs source-scoped routing must receive the same source-file contract; the renderer should materialize it, not invent it from interface names.
- state=fixed-locally | target=runtime-routed-prefix-policy-to-wan-static-ipv4-sources | evidence=2026-05-22 full-loop hostile IPv4 still failed after IPv6 routed-prefix source files were fixed. Live `nixos-router-upstream` first selected `pol-hostile-ew` until a route hotpatch forced table 2007 toward `core-nebula`; then Hetz `hetz-router-upstream ip route get 1.1.1.1 from 10.20.70.100 iif policy-dmz-wan` returned unreachable. Adding only `ip rule add from 10.20.70.0/24 iif policy-dmz-wan table 2004 priority 1994` made `nixos-hostile-node01 ping -4 1.1.1.1` pass and traceroute show the expected NixOS -> Nebula -> Hetz path. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` now requires the Hetz `policy-dmz-wan -> core` public-egress forwarding rule to carry static source prefix `10.20.70.0/24` alongside dynamic IPv6 source files. | reason=CPM owns the explicit source scope for routed-prefix public egress. Static IPv4 routed prefixes and dynamic IPv6 source-file prefixes are the same forwarding contract; the renderer should materialize both, not rediscover IPv4 source scope from runtime routes.
- state=fixed-locally | target=network-labs-route-duplicate-sweep | evidence=2026-05-23 `NETWORK_REPO_DIRECT_TEST_OK=1 NETWORK_REPO_SWEEP=1 TEST_JOBS=$(nproc) bash tests/test-network-labs-inventory-sweep.sh` no longer reports `route-duplicate`; CPM now normalizes route-augmented runtime targets after service/DNS route injection, and the route report keys `intent.kind` and `intent.source` separately so policy-table complements are not collapsed into false positives. The sweep runner now compiles network-labs outputs concurrently and fails hard if any jq report crashes. | reason=CPM route identity and dedupe must preserve semantic lane differences without emitting duplicate equivalent routes on the same realized interface/table. This belongs in CPM route materialization, not in renderer filtering or local NixOS cleanup.
- state=solved | target=overlay-dns-lane-policy-missing-policy-overlay-default | evidence=2026-05-23 CPM sweep originally failed only `default-route-lane` for `examples/s-router-overlay-dns-lane-policy` site-c after a rejected CPM role-gated route synthesis was removed. `gron`/jq proved the missing `access=c-router-access-client,uplink=east-west` default was absent from NFM, not lost by CPM. NFM commit `a475aca` now emits explicit overlay access-uplink default-reachability; CPM lock points at that commit and `NETWORK_REPO_DIRECT_TEST_OK=1 NETWORK_REPO_SWEEP=1 TEST_JOBS=$(nproc) bash tests/test-network-labs-inventory-sweep.sh` passes for 44 outputs. | reason=NFM owns executable forwarding consequences for allowed external overlay lanes. CPM must preserve those route facts and must not synthesize them from target roles such as policy/upstream-selector or generated link names.
- state=solved | target=example-upstream-policy-default-route-preservation | evidence=2026-05-18 live `s-router-test` `/etc/network-artifacts/forwarding.json` emitted lane-specific `0.0.0.0/0` and `::/0` defaults for `nixos-router-upstream` on `isp-a`, `isp-b`, and hostile `east-west`; live `/etc/network-artifacts/control-plane.json` had collapsed those route facts before the fix; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` passes after preserving semantic route identity. | reason=CPM must join and preserve explicit NFM route facts; route dedupe must not collapse distinct policy lanes that share destination and next hop.
- state=still-broken | target=example-explicit-simulated-isp-nat66 | evidence=2026-05-21 code inspection found `src/cpm/firewall-intent/nat.nix` still hardcodes `nat6Enabled = false`, while live normal NixOS clients have only ULA source addresses for public IPv6 egress through simulated ISP VLAN uplinks and fail `ping -6 google.com`. | reason=CPM is the layer that turns explicit NFM/lab egress semantics plus inventory host-uplink realization into `natIntent`. NAT66 must be opt-in, source-scoped, and warn/audit loudly; CPM must not enable NAT66 for hostile routed GUA tenants or for real routed-prefix Hetzner paths.
- state=still-broken | target=example-dmz-dns-internal-only-contract | evidence=2026-05-21 live `nixos-dmzweb01 dig google.com` succeeds even though direct public DNS and generic public egress are blocked. The observed path is DMZ endpoint -> access DMZ resolver -> `site-dns-mgmt` -> public forwarders, matching current CPM DNS service contracts instead of an nft bypass. | reason=CPM DNS service output needs an explicit contract that distinguishes internal-only DNS from public-recursive DNS. Renderers should only materialize `services.dns.forwarders`, `allowFrom`, views/zones, and leak-prevention data supplied by CPM, not infer DMZ split-horizon policy locally.
- state=fixed-locally | target=example-hostile-ipv4-public-egress | evidence=2026-05-22 live Hetzner hotpatches proved the missing durable facts: `hetz-router-upstream` needed overlay ingress table default `0.0.0.0/0 via 10.80.0.14 dev pol-dmz-ew`, `hetz-router-upstream` needed the east-west policy lane default retained, and `hetz-router-core` needed `10.20.70.0/24 via 10.80.0.5 dev upstream`. NFM now emits remote overlay return routes on non-overlay WAN cores, CPM preserves the route facts and adds overlay ingress/policy lane defaults, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` passes after bumping the NFM lock. | reason=The live hack was not NixOS topology state. It was missing forwarding/control-plane route semantics for public egress that enters Hetz over east-west and exits via WAN.
- state=fixed-locally | target=example-normal-and-hostile-exposure-lanes | evidence=2026-05-21 full-loop live validation showed `clab-client01` and `clab-streaming01` returned `Destination Net Unreachable` for public IPv4 because CPM kept only the first upstream policy default per shared WAN next-hop. The patched CPM kernel-default dedupe key preserves `policyOnly` lane identity for default routes, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` now covers NixOS dual-ISP defaults, CLAB client/streaming WAN IPv4 defaults, CLAB delegated client IPv6 east-west egress, and hostile east-west defaults. | reason=Default-route dedupe is a CPM semantic identity operation. Destination-only normalization loses policy lane meaning supplied by NFM and makes renderers unable to materialize the modeled household VLAN exposure correctly.
- state=fixed-locally | target=example-clab-client-ipv6-wan-default | evidence=2026-05-21 live `s-router-clab` showed `clab-client01` IPv6 died at downstream/policy/upstream because the CPM artifact omitted the client WAN `::/0` policy default while preserving admin and streaming. A narrow live hotpatch adding only the client IPv6 policy defaults on downstream -> policy -> upstream made `clab-client01 ping -6 2606:4700:4700::1111` pass. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` now requires `clab-router-access-client` in the CLAB WAN IPv6 default lane set. | reason=CPM must not drop a modeled WAN IPv6 default merely because the tenant also has a runtime routed prefix. The selected lane and explicit egress/NAT66 intent decide whether NAT or routed public egress is valid; renderers must receive the explicit route fact instead of inventing a recovery route.
- state=fixed-locally | target=example-downstream-local-service-forwarding | evidence=2026-05-21 full-loop failed `test-examples-three-site-allowed-flows` on `hetz-router-access-client dig -b 10.90.20.1 @10.90.10.1 example.com A`. Live route/nft tracing showed `hetz-router-downstream` had the connected access routes in CPM, but its forwarding intent only allowed access-to-policy pairs, so renderer policy tables omitted the peer local tenant route and nft dropped `access-client -> access-dmz` DNS. Hotpatching table `2000` with `10.90.10.0/24 via 10.80.0.2`, table `2001` with `10.90.20.0/24 via 10.80.0.0`, and narrow DNS nft accepts made the exact dig pass. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-downstream-local-service-forwarding.sh` now requires CPM to emit Hetz client-to-DMZ DNS plus CLAB downstream same-selector DNS and Cast service pairs before any renderer can materialize routes or nft. | reason=Local same-selector tenant/service relations are CPM semantics. The renderer can materialize routes and nft only from explicit forwarding intent; it must not infer local cross-access DNS or Cast lanes from interface names or connected prefixes.
- state=fixed-locally | target=example-policy-table-internal-route-complements | evidence=2026-05-21 full-loop live validation then reduced to CLAB Cast TCP failures: `clab-router-policy` had nft accept for client `eth2` to streaming `eth6`, but `ip route get 10.50.50.10 from 10.50.20.10 iif eth1` selected table `1001` and WAN `eth7` because CPM emitted only lane defaults for Linux policy tables, not the internal route complements. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-overlay-upstream-policy-defaults-preserved.sh` now asserts the client policy lane carries `10.50.50.0/24 via 10.50.0.26` and the streaming lane carries `10.50.20.0/24 via 10.50.0.18` as explicit `policy-table-internal-reachability` routes. | reason=Once CPM emits an ingress-lane policy default, it also owns the renderer-independent route facts needed for that policy table to preserve internal intent before defaulting to WAN; renderers must not invent these complements from names or from local topology guesses.
- state=solved | target=example-policy-routing-source-prefix-scope | evidence=2026-05-21 full-loop reached `test-examples-three-site-hostile-overlay-egress-path` and exposed broad live rules such as `12001: from all iif downstr-client lookup 2001`; follow-up `gron` checks proved NFM emits `tenantPrefixOwners` plus hostile east-west `policy-derived-default` lanes, and CPM preserves those exact fields. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-source-scope-preserved.sh` passes. | reason=CPM is not the first-bad layer for this bug: it preserves the NFM source-prefix ownership and lane contract. The remaining bug belongs in renderer materialization, which must emit `from <tenant-prefix> iif <lane>` rules from explicit CPM data.
- state=fixed-locally | target=overlay-underlay-endpoint-source-routes | evidence=2026-05-23 live `s-router-test` and `gron` comparison showed the overlay core must route explicit Hetz underlay endpoint source files over its modeled access-underlay default, not over the overlay/upstream lane. CPM now projects endpoint `sourceFile` routes only onto p2p interfaces that already carry a modeled default, so `esp.nixos` core-nebula uses `p2p-nixos-router-access-client-nixos-router-core-nebula` via `10.10.0.2` / `fd42:dead:beef:1000::2`, and `esp.hetz` upstream uses `p2p-hetz-router-core-hetz-router-upstream` via `10.80.0.6` / `fd42:dead:cafe:1000::6`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-runtime-underlay-endpoint-source-routes.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-upstream-selector-nebula-underlay-core-transit.sh` pass. | reason=CPM must project explicit overlay underlay endpoint source files onto the modeled default-bearing underlay path while preserving the narrow firewall contract. Renderers must not invent host routes, broad core-to-WAN bypasses, or overlay bootstrap routes from node names.
- state=solved | target=access-dns-router-self-listeners | evidence=2026-05-19 live Hetzner `hetz-router-access-dmz` rendered unbound only on loopback and validation failed `dig @10.90.10.1`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-dns-router-self-listeners.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-policy-routes.sh` pass after CPM merges router-self advertised DNS listeners into existing access DNS services and carries the direct-DNS egress block. | reason=CPM owns the explicit service contract; renderers should only materialize `services.dns.listen`, `allowFrom`, `forwarders`, source interfaces, and leak-prevention flags already present in CPM.
- state=fixed-locally | target=static-uplink-egress-routes | evidence=2026-05-20 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-static-uplink-egress-routes.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-realized-interface-routes.sh` pass after CPM wires `inventory.controlPlane.sites.*.*.uplinks.*.egress.static.routes` into explicit and synthetic WAN runtime interfaces. | reason=Static WAN next hops are inventory realization facts, but CPM must carry them into runtime route contracts so renderers do not invent default gateways.
- state=still-broken | target=structural-keyword-boundary-hard-gate | evidence=2026-05-20 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-structural-keyword-boundary.sh` still fails across existing CPM implementation files with grouped matches for role/structure keywords, abbreviations, lane names, generated-id fragments, and string parsing primitives. | reason=The gate is concrete and testable, but the current CPM implementation still contains broad structural coupling; fixing it requires a larger S88-style decomposition rather than weakening the test or adding file LOC/keyword exceptions.
- state=fixed-locally | target=example-dns-service-wan-forwarders | evidence=2026-05-19 full loop failed at `site-c-ipv4-a` because live `hetz-router-access-dmz` had correct routes, listener, nft input, and WAN egress, but CPM emitted no `services.dns.forwarders`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-example-dns-service-wan-forwarders.sh` now passes after CPM derives DNS service WAN forwarders from the service-to-external DNS relation. | reason=DNS upstream policy is intent; inventory may select the DNS implementation, but must not be the only place where a modeled DNS service learns it may use WAN resolvers.
- state=still-broken | target=tri-site-dual-wan-overlay-integration-bgp-fixture | evidence=2026-05-19 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-passing-fixtures.sh` still fails on `forwardingModel.enterprise.esp0xdeadbeef.site.site-a.overlayReachability.east-west.nodes.s-router-core-nebula.addr4` missing in that older fixture inventory. | reason=This is an unrelated fixture inventory contract violation, not a DNS listener or tenant gateway regression.
