# network-control-plane-model regression state

- state=solved | target=explicit-access-dns-forwarders-vs-policy-derived-site-dns | evidence=2026-05-16 live `s-router-test` inventory for `esp-nixos-router-access-client` has DNS `forwarders = [ "1.1.1.1" "9.9.9.9" "2606:4700:4700::1111" "2620:fe::fe" ]`, but the live CPM/rendered target changed that service to `forwarders = [ "10.20.10.1" "fd42:dead:beef:10::1" ]`, `blockDirectEgress = true`, and `policyMatrix` only for the site DNS addresses. CPM now preserves explicit access DNS forwarders ahead of policy-derived site-DNS forwarders; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-derived-dns-upstreams.sh` passes and verifies explicit public forwarders stay present while derived site-DNS forwarders are not substituted. The downstream NixOS renderer was checked and consumes `runtimeTarget.services.dns.forwarders` directly into Unbound `forward-addr`, so no renderer-side invention was patched. | reason=Explicit runtime DNS forwarders are CPM input contract data; policy-derived site DNS is fallback enrichment only and must not override explicit access service configuration.

- state=solved | target=runtime-hotpatch-explicit-dns-forwarders | evidence=2026-05-16 working live hotpatch stopped generated `unbound`, wrote `/run/unbound-codex-explicit-forwarders.conf` with `forward-addr: 1.1.1.1`, `9.9.9.9`, `2606:4700:4700::1111`, and `2620:fe::fe`, then ran `unbound -d -c /run/unbound-codex-explicit-forwarders.conf` manually on the NixOS access DNS containers. Matching nft hotpatches added `temp-live-explicit-access-dns-output` in each access router output chain and `temp-live-explicit-access-dns-forwarders` in `nixos-router-policy` for DNS service source addresses only. Endpoint validation returned `NOERROR` for `dig @10.20.15.1`, `@10.20.20.1`, `@10.20.30.1`, and `@10.20.50.1` against `cache.nixos.org A`. The durable CPM fix now emits the same explicit forwarder shape without requiring renderer-side DNS inference. | reason=The live hotpatch identified the correct runtime contract, and CPM now preserves that contract while keeping direct-client DNS leak prevention modeled separately.

- state=solved | target=missing-runtime-target-realization-hard-fail | evidence=2026-05-16 stale s-sigma lab artifact showed renderer route output could be blocked by unresolved inventory realization coverage. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-missing-runtime-target-realization.sh` removes `policy-runtime` from the minimal explicit fixture inventory and verifies CPM hard-fails with `inventory.nix must explicitly realize every control_plane_model runtime target` while naming `policy-1`. | reason=Missing runtime target realization is a CPM inventory contract violation, not something the NixOS renderer should repair or silently skip before route emission.
